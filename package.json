{
  "name": "jobblast",
  "version": "0.0.0",
  "description": "Self-hosted, single-user, $0 job-application assistant: aggregates real postings from free APIs, scores them against your profile, and drafts a tailored resume + cover letter for you to review before you apply.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yaniswav/jobblast.git"
  },
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "setup": "pnpm --filter @workspace/scripts run setup",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "dev:api": "pnpm --filter @workspace/api-server run dev",
    "dev:web": "pnpm --filter @workspace/jobblast run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "test": "pnpm --filter @workspace/api-server run test"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
